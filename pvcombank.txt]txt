// SYNC, 1 thread — cách viết mặc định, đơn giản nhất
Account acc = accountRepo.findById(id);   // block ngay tại đây, cùng 1 thread đợi

// ASYNC nhưng vẫn có thể chỉ dùng 1 thread (non-blocking I/O, không cần tạo thread mới)
webClient.get().uri(url).retrieve().bodyToMono(AccountDto.class)
    .subscribe(dto -> System.out.println(dto));   // gọi xong chạy tiếp ngay, kết quả xử lý sau qua callback
    .subscribe(dto -> System.out.println(dto));
    

// ASYNC bằng cách dùng multithreading — cách phổ biến hơn trong code Java truyền thống
@Async
public void sendEmailAsync(String to) {
    emailService.send(to);   // chạy trên thread khác (từ thread pool), thread gọi không đợi
}
